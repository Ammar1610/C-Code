#include <stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node *links;
}
int main(){
    struct node *head=malloc(sizeof(struct node))
    printf("Enter the data of first node")
    scanf("%d",&c);
    head->data=c;
    head->link=NULL;
    structnode *counter = mall

        return 0;
    }
    